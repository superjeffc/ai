{
  "name": "ai-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker AI assistant migrated to git repository.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.111.0"
  }
}
